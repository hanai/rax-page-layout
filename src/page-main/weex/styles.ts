import { CSSProperties } from '@rax-types/rax';

const styles: { [key: string]: CSSProperties } = {
  container: {
    flex: 1,
    overflow: 'hidden',
  },
  scrollView: {
    flex: 1,
  },
};

export default styles;
